---
name: lint
description: Run ctxgrd lint to check for broken references and rule violations in structured markdown records.
---
# ctxgrd lint

Run the `ctxgrd` linter to identify issues in your document graph.

## Usage

```bash
ctxgrd lint
```

If you want machine-readable output:

```bash
ctxgrd lint --format json
```
