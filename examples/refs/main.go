// Demo Go file for the reference scanner. The supersededTopic line
// is suppressed by the marker on the line above; futureTopic fires;
// eventSourcingDecision points at a real document and stays silent.

package audit

// ctxgrd: ignore-next
const supersededTopic = "ADR-1234"

const futureTopic = "ADR-9999"

const eventSourcingDecision = "ADR-001"
