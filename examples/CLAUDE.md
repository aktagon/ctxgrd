---
title: Example agent guide
---

# Example agent guide

Always-loaded instructions for agents working in this fixture repo.
Path-claimed by the `[AGENTS]` namespace — an id-less file-level
document, linted by the `agents.*` rules and rendered by `ctxgrd serve`
at `/file/CLAUDE.md` (ADR-103).

- The audit-trail decision lives in ADR-001; read it before touching
  the event store.
- Run `ctxgrd --root examples` to lint this fixture.

## Conventions

Keep documents small and typed; the requirements are in PRD-001.
